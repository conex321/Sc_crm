---
phase: 01-pipedrive-design-system
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - components/ui/button.tsx
  - components/ui/badge.tsx
  - components/ui/card.tsx
  - components/ui/table.tsx
  - components/ui/tabs.tsx
  - components/ui/input.tsx
  - components/ui/select.tsx
  - components/ui/textarea.tsx
  - components/crm/customer-status-badge.tsx
  - components/crm/import-wizard.tsx
  - app/(dashboard)/dashboard/page.tsx
  - app/(dashboard)/campaigns/page.tsx
  - app/(auth)/login/page.tsx
  - app/(dashboard)/settings/integrations/page.tsx
  - lib/format.ts
  - lib/crm/dashboard.ts
  - components/crm/pipeline-board.tsx
  - components/crm/opportunity-list.tsx
  - components/crm/document-list.tsx
  - components/crm/line-items-editor.tsx
  - app/(dashboard)/opportunities/[id]/page.tsx
  - app/(dashboard)/settings/catalog/page.tsx
  - app/(dashboard)/settings/catalog/packages/[id]/edit/page.tsx
autonomous: true
requirements: [DSGN-04, DSGN-05]
must_haves:
  truths:
    - "Zero hardcoded palette classes (amber/emerald/sky/slate/red-*) remain in the six blast-radius files — all banners/chips/progress use pd-* semantic utilities and flip correctly in dark mode"
    - "Every money value in the app renders through lib/format.ts — CAD default, per-row currency respected; no en-US/USD Intl.NumberFormat literal remains outside lib/format.ts"
    - "Buttons/inputs are 4px radius h-8, cards 8px no shadow, tables have gray header + row hover, badges are pills — per UI-SPEC Section 5, light and dark"
    - "tsc + build + 17-route e2e walk + Playwright pass (kanban drag-drop included) all green before deploy"
  artifacts:
    - path: "lib/format.ts"
      provides: "Shared fmtMoney/fmtCad house formatter"
      exports: ["fmtMoney", "fmtCad"]
    - path: "components/crm/customer-status-badge.tsx"
      provides: "Status badge on pd label/status tokens (no emerald/amber/slate)"
  key_links:
    - from: "components/crm/opportunity-list.tsx (and 5 sibling USD sites)"
      to: "lib/format.ts"
      via: "import { fmtMoney } / { fmtCad }"
      pattern: "from \"@/lib/format\""
    - from: "components/crm/pipeline-board.tsx"
      to: "lib/format.ts"
      via: "local fmtMoney deleted, imported instead (sumByCurrency stays local)"
    - from: "tinted banners (dashboard, campaigns, login, import-wizard, integrations)"
      to: "app/globals.css --pd-* tokens"
      via: "bg-pd-{tone}-bg-light / border / text-pd-{tone}-strong utilities from PLAN-01"
---

<objective>
Finish the reskin: apply the UI-SPEC component styling deltas to the shadcn kit, sweep the six files with hardcoded palette classes onto the PLAN-01 semantic tokens (DSGN-04), consolidate money rendering into a shared CAD-default formatter replacing ALL SIX legacy USD sites (DSGN-05), then run the full verification gate including kanban drag-drop and a dark-mode parity pass.

Purpose: after this plan the app is coherently Pipedrive in both themes with zero functional regression; deploy follows (orchestrator step).
Output: restyled `components/ui/*` variants, six swept feature files, new `lib/format.ts` + six formatter call-site updates, green verification gate.
</objective>

<context>
@.planning/phases/01-pipedrive-design-system/01-UI-SPEC.md   ← Section 5 (component deltas), Section 7 (dark parity), Money formatting section
@.planning/phases/01-pipedrive-design-system/01-RESEARCH.md  ← Finding 2 (blast-radius table with line numbers), Finding 4 (six USD sites + lib/format.ts code), Finding 5 (Playwright eyeball list)
@.planning/phases/01-pipedrive-design-system/01-01-SUMMARY.md ← what PLAN-01 shipped (token names, deviations)
@.planning/codebase/CONVENTIONS.md

**Non-negotiable rules:**
1. Depends on PLAN-01's tokens (`pd-*` utilities, `--pd-primary-hover/active`, elevations) — do not redefine tokens here; if one is missing, add it to `:root` AND `.dark` in the same commit.
2. `components/ui/*` edits are classNames-only inside existing variant strings — no API/prop/structure changes; every consumer keeps compiling untouched.
3. `pipeline-board.tsx`: classNames-only diff. Never touch `DndContext`, sensors, refs, or handlers — kanban drag-drop is the highest-risk regression surface (RESEARCH Risk 2).
4. Restyle without rewording on e2e-checked routes. If copy must change, update the matching regex in `scripts/e2e-rayan.mts` in the same task.
5. Formatter sweep changes RENDERING only. Do NOT touch `"USD"` data defaults in forms/zod schemas/DB columns (opportunity-form.tsx:178, product-form.tsx:82, actions.ts schemas, lib/db/schema.ts) — that is a data-behavior decision flagged to Matthew, out of scope (RESEARCH Open Question 2).
6. Deliberately KEEP: `bg-black/50` dialog/sheet overlays and `text-white` on destructive variants (correct on Pipedrive red).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Component styling deltas on the shadcn kit (UI-SPEC Section 5)</name>
  <files>components/ui/button.tsx, components/ui/badge.tsx, components/ui/card.tsx, components/ui/table.tsx, components/ui/tabs.tsx, components/ui/input.tsx, components/ui/select.tsx, components/ui/textarea.tsx</files>
  <action>
ClassNames-only edits inside the existing cva/variant strings. Light values named; dark comes free via the vars.

- **button.tsx**: all variants → radius 4px (`rounded-[4px]` — do not rely on `rounded-md`, which is 6px now that `--radius: 8px`). `default` (primary): add `shadow-pd-button hover:bg-[var(--pd-primary-hover)] active:bg-[var(--pd-primary-active)]` (replace any `hover:bg-primary/90`). `destructive`: `hover:bg-[#c82627] active:bg-[#b21019]` — use pd vars if PLAN-01 exposed them, else these literals are the spec'd exception; verify white-on-`--destructive` stays AA. `outline`: `bg-card border-[var(--input)] hover:bg-secondary active:bg-accent`. `ghost`: `hover:bg-accent`. Focus: `focus-visible:ring-[3px] focus-visible:ring-ring/40`. Keep heights (`h-8` for sm) and font weight 600 label (`font-semibold` if not already).
- **badge.tsx**: base → pill: `rounded-full h-5 px-2 text-xs font-semibold` (12px/600). `outline` variant: `border-[rgba(33,35,44,.24)]`-equivalent via `border-border text-pd-text-secondary` (divider-strong: use `border-[var(--pd-divider-strong)]` only if PLAN-01 defined it; otherwise `border-border`). Do NOT add color variants — call sites use `bg-pd-label-{color}-bg text-pd-label-{color}-fg` utilities directly (Task 2).
- **card.tsx**: `rounded-lg` (8px) `border bg-card`, REMOVE default `shadow-sm`/`shadow-xs`. (Kanban cards get `shadow-pd-raised hover:shadow-pd-raised-hover` at the call site in pipeline-board — one-line className addition there, nothing else.)
- **table.tsx**: header row `bg-secondary`; header cells `text-xs font-semibold text-pd-text-secondary` (12px/600, sentence case — do NOT uppercase); body rows keep `border-b` + existing compact `py-2 text-sm` padding; row hover `hover:bg-secondary`. If the primitive hardcodes `hover:bg-muted/50`, swap to `hover:bg-secondary`.
- **tabs.tsx**: underline style — TabsList: `bg-transparent border-b border-border rounded-none p-0 gap-4` (drop the pill `bg-muted` look); TabsTrigger: `rounded-none px-0 pb-2 text-sm font-semibold text-pd-text-secondary data-[state=active]:text-foreground data-[state=active]:border-b-2 data-[state=active]:border-primary data-[state=active]:shadow-none hover:text-foreground`.
- **input.tsx / select.tsx (trigger) / textarea.tsx**: `bg-card border-[var(--input)] rounded-[4px] h-8` (textarea keeps auto height), `placeholder:text-pd-text-muted`, focus `focus-visible:border-[var(--pd-info)] focus-visible:ring-[3px] focus-visible:ring-ring/25`. Keep 14px text and existing paddings — density is locked.
- Dropdown/popover/dialog shadows: if trivial, swap their `shadow-md/lg` for `shadow-pd-floating` (menus/popovers) and `shadow-pd-overlay` (dialog content). Skip sonner restyling — `richColors` is acceptable per spec tolerance; note in SUMMARY if skipped.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
    Browser: /accounts/new form (inputs/selects/buttons at 4px radius, blue focus ring), /accounts/[id] tabs underline style, any table header gray + hover, no card shadows.
  </verify>
  <done>Kit matches UI-SPEC Section 5 in light + dark; all consumers compile with zero call-site changes; controls still h-8, rows still py-2.</done>
</task>

<task type="auto">
  <name>Task 2: Hardcoded-color sweep — six files onto semantic tokens (+ dark parity)</name>
  <files>components/crm/customer-status-badge.tsx, components/crm/import-wizard.tsx, app/(dashboard)/dashboard/page.tsx, app/(dashboard)/campaigns/page.tsx, app/(auth)/login/page.tsx, app/(dashboard)/settings/integrations/page.tsx, components/crm/pipeline-board.tsx</files>
  <action>
Replace every hardcoded palette class per the RESEARCH Finding 2 table. Standard patterns (utilities from PLAN-01's bridge — they flip in dark automatically, so DELETE the paired `dark:` palette classes as you go):
- **Tinted banner** (the recurring `border-amber-300/60 bg-amber-50 text-amber-900 dark:...` idiom) → `border-pd-{tone}-bg bg-pd-{tone}-bg-light text-pd-{tone}-strong` where tone ∈ warning|info|negative|positive. If border contrast looks off, `border` (divider) is acceptable — record choice in SUMMARY and use it consistently across all banners.
- **Status chip** → `bg-pd-label-{color}-bg text-pd-label-{color}-fg` pairs.

Per file:
- `customer-status-badge.tsx`: emerald→positive/green label pair, amber→warning/yellow pair, slate→gray pair (`bg-pd-label-gray-bg text-pd-label-gray-fg`). Remove all `dark:` shade classes — tokens handle it.
- `import-wizard.tsx` (~lines 244, 320, 410): two amber banners → warning banner pattern; `bg-emerald-500` progress bar → `bg-pd-positive`.
- `dashboard/page.tsx` (~42-43, 59, 67, 184): sky info banner → info pattern; red banner + `text-red-600` → negative pattern / `text-pd-negative-strong`; amber banner → warning pattern.
- `campaigns/page.tsx` (~76, 81): amber banner + chip → warning banner + yellow label pair.
- `login/page.tsx` (~43): amber banner → warning pattern.
- `settings/integrations/page.tsx` (~60): emerald "connected" banner → positive pattern.
- `pipeline-board.tsx`: classNames ONLY — add `shadow-pd-raised hover:shadow-pd-raised-hover` to the kanban card wrapper; column/board surfaces onto `bg-card`/`bg-secondary` if any raw grays exist. Do not touch dnd-kit code.

Then dark-parity pass: `grep -rE "(amber|emerald|sky|slate)-[0-9]" app components` must return zero UI-class hits (allow non-class matches); toggle dark and eyeball each swept surface — banners and chips legible, no light-hex flashes.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
    Plus the grep above returning empty, and browser check of all six surfaces in light AND dark.
  </verify>
  <done>Zero hardcoded palette classes in the six files; banners/chips render on semantic tokens and are legible in both themes; kanban cards have raised elevation with drag untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Shared CAD formatter — lib/format.ts replacing all six USD sites</name>
  <files>lib/format.ts, lib/crm/dashboard.ts, components/crm/pipeline-board.tsx, components/crm/opportunity-list.tsx, components/crm/document-list.tsx, components/crm/line-items-editor.tsx, app/(dashboard)/opportunities/[id]/page.tsx, app/(dashboard)/settings/catalog/page.tsx, app/(dashboard)/settings/catalog/packages/[id]/edit/page.tsx</files>
  <action>
1. Create `lib/format.ts` (RESEARCH Finding 4, lifted from the blessed `pipeline-board.tsx:25-32` pattern):
```ts
// House currency is CAD (QuickBooks). Per-row currencies format in their own
// currency; locale follows currency (matches pipeline-board behavior).
export function fmtMoney(amount: number, currency = "CAD"): string {
  const locale = currency === "USD" ? "en-US" : "en-CA";
  return new Intl.NumberFormat(locale, {
    style: "currency", currency, maximumFractionDigits: 0,
  }).format(amount);
}
export const fmtCad = (n: number) => fmtMoney(n, "CAD");
```
2. `components/crm/pipeline-board.tsx`: delete the local `fmtMoney`, import from `@/lib/format` (`sumByCurrency` stays local — it's opportunity-shaped). ClassNames/imports only near dnd-kit code.
3. `lib/crm/dashboard.ts` (~line 315): replace the local `fmtCad` implementation with `export { fmtCad } from "@/lib/format";` (or import + re-export) so `dashboard/page.tsx`'s existing import keeps working.
4. Sweep ALL SIX legacy USD sites — delete each module-level `new Intl.NumberFormat("en-US", { ... currency: "USD" ...})` and call `fmtMoney(amount, row.currency)` where the row carries a `currency` field (opportunity rows, line items, catalog products do), else `fmtCad(amount)`:
   - `components/crm/opportunity-list.tsx:8`
   - `components/crm/document-list.tsx:29`
   - `components/crm/line-items-editor.tsx:15`  ← missed by CONTEXT, confirmed by research
   - `app/(dashboard)/opportunities/[id]/page.tsx:18`
   - `app/(dashboard)/settings/catalog/page.tsx:10`
   - `app/(dashboard)/settings/catalog/packages/[id]/edit/page.tsx:17`  ← missed by CONTEXT, confirmed by research
5. Add `tabular-nums` to money cells in the tables touched here (opportunity-list, document-list, catalog list, line-items editor) per UI-SPEC Money section.
6. Do NOT change USD data defaults (forms/schemas/DB) — rendering only.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
    Plus `grep -rn "en-US" app components lib --include=*.tsx --include=*.ts` shows no formatter outside `lib/format.ts`, and browser spot-check: /opportunities list + detail, /settings/catalog show `CA$` (or `US$` only on genuinely-USD rows).
  </verify>
  <done>One shared formatter; all six legacy sites swept; CAD-default with per-row currency respected; money cells tabular-nums; no data-write behavior changed.</done>
</task>

<task type="auto">
  <name>Task 4: Full verification gate (local)</name>
  <files>none (verification only; fixes go to the file that owns the bug)</files>
  <action>
Run the phase gate in order; fix-forward any failure and re-run from the failed step:
1. `npx tsc --noEmit`
2. `npm run build`
3. Start the dev server (note the port), then `NEXT_PUBLIC_SITE_URL=http://localhost:<port> npx tsx scripts/e2e-rayan.mts` (needs `E2E_LOGIN_PASSWORD` in `.env.local`) — all 17 routes must pass.
4. Playwright browser pass (RESEARCH Finding 5 priority list), light theme:
   a. `/opportunities` kanban — DRAG a card between stages; confirm optimistic move + persisted stage after reload + toast. Highest-risk check.
   b. `/dashboard` — KPI cards, info/negative/warning banners on new tokens, charts on re-pointed `--chart-*`.
   c. `/accounts` — 5.5k-row table density unchanged, header gray, row hover.
   d. `/accounts/[id]` — tabs underline style, TaskComposer/NoteComposer, customer-status badge.
   e. `/accounts/import` — warning banners + positive progress bar.
   f. Top bar: quick-add all four entries navigate; search field opens ⌘K dialog; sidebar collapse; footer SHA.
   g. `/login` — restyled banner.
   h. One form page: dialog/select overlays + blue focus rings.
5. Dark-mode toggle check: switch to Dark via the user menu; re-eyeball (a)–(d) + one banner surface — no white flashes, chips legible, nav active tint visible, then switch back.
6. Confirm the "Phase 1 · sha" footer renders and no e2e copy regressed.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npx tsx scripts/e2e-rayan.mts</automated>
    (e2e with dev server running and NEXT_PUBLIC_SITE_URL set as above.) Playwright checklist above fully walked, light + dark.
  </verify>
  <done>All gates green locally; kanban drag-drop works; dark parity confirmed; phase ready for deploy.</done>
</task>

</tasks>

<verification>
- tsc + build green; e2e walk 17/17 against local dev.
- Zero hardcoded palette classes (grep) and zero USD formatters outside lib/format.ts (grep).
- Playwright priority list complete in light AND dark, kanban drag-drop included.
- Density unchanged: 14px root, h-8 controls, py-2 rows (compare /accounts visually).
</verification>

<success_criteria>
- DSGN-04: every existing screen coherent in the Pipedrive language, both themes, no functional regression.
- DSGN-05: every money value renders through `lib/format.ts` (CAD default, per-row currency respected).
- Full local gate green — phase verifiable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipedrive-design-system/01-02-SUMMARY.md` (files swept, formatter call-site map, gate results incl. dark pass, any spec deviations, and the USD-data-defaults flag for Matthew — RESEARCH Open Question 2).

**Orchestrator checklist AFTER verification (not executor tasks):**
- [ ] Deploy: `npx vercel --prod --yes`, then re-run e2e against `https://sc-crm-sand.vercel.app`
- [ ] Report to Matthew (include the USD data-defaults question: flip form/schema/DB defaults to CAD?)
- [ ] `update-project-notes` skill (D-/F- IDs as needed)
</output>
